# -------------------------------------------------------
# COUNTERCASE Environment Variables
# -------------------------------------------------------
# Copy this file to `.env` and fill in your keys.
# The `.env` file is loaded automatically by settings.py
# and is git-ignored so secrets are never committed.
# -------------------------------------------------------

# -- Mistral API Configuration --
# Get your API key from: https://console.mistral.ai/api-keys
LLM_API_KEY=
LLM_API_URL=https://api.mistral.ai/v1
LLM_API_MODEL=open-mistral-7b

# -- Optional: Local LLM (if running Mistral locally) --
# LOCAL_LLM_MODEL=mistralai/Mistral-7B-Instruct-v0.2

# -- HuggingFace (optional, for gated models) --
# HF_TOKEN=
